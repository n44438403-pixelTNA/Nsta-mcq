#!/bin/bash

# A helper to find files containing renderMathInHtml(q.question) or q.question in dangerouslySetInnerHTML
# and update them to include a statements block.
# We'll just edit the files directly since there are a few.
